block img, default:{
    var ctx = applyNext(),
        data = this.data;

    // чтобы была возможность использовать переменные в качестве фриза,
    // см. http://bem.info/articles/borschik/#dynamic-urls
    ctx.imageUrl = borschik.link('img.png');
    console.log(borschik.link('img.png'));

    return applyCtx(ctx);
}
